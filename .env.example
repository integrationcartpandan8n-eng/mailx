# === Server ===
PORT=3000
NODE_ENV=development
WEBHOOK_SECRET=change-me
ADMIN_PASSWORD=mailx2026

# === PostgreSQL (Docker) ===
DATABASE_URL=postgresql://mailx:mailx_secret@localhost:5432/mailx

# === ActiveCampaign (fallback global — credenciais por cliente ficam no DB) ===
AC_API_URL=
AC_API_KEY=

# === ActiveCampaign Automation IDs (global) ===
AC_AUTOMATION_COMPRA_APROVADA=
AC_AUTOMATION_CARRINHO_ABANDONADO=

# === Digistore24 (fallback global — passphrase por cliente fica em store_integrations.api_token) ===
DS24_IPN_PASSPHRASE=

# === Google Drive ===
GOOGLE_SERVICE_ACCOUNT_PATH=./credentials/google-sa.json
GOOGLE_DRIVE_ROOT_FOLDER_ID=

# === Domínios ===
API_DOMAIN=api.mailxgroup.com
APP_DOMAIN=app.mailxgroup.com
SENDING_DOMAIN=envio.mailxgroup.com
